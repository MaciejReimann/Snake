{
  "name": "snake",
  "version": "1.0.0",
  "description": "Snake Game",
  "engines": {
    "node": "9.10.0"
  },
  "scripts": {
    "test": "jest --watch",
    "start": "node server",
    "server": "nodemon server",
    "build": "cd client && cd public && browserify scripts/index.js -o bundle.js",
    "publish": "git push heroku master && heroku open"
  },
  "cacheDirectories": [
    "node_modules"
  ],
  "author": "Maciej Reimann",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^4.1.0",
    "express": "^4.16.3",
    "jest": "^23.6.0"
  },
  "devDependencies": {
    "nodemon": "^1.18.4"
  }
}